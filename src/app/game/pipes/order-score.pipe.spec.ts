import { OrderScorePipe } from './order-score.pipe';

describe('OrderScorePipe', () => {
  it('create an instance', () => {
    const pipe = new OrderScorePipe();
    expect(pipe).toBeTruthy();
  });
});
